import {test} from "@playwright/test";

test.describe("ds", () => {
    test("dsa",async ({page,browser},testInfo) =>{
        testInfo.status
         const context = await browser.newContext();
         context.pages()
         context.browser()
    })
})